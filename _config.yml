# RadExplain — GitHub Pages Configuration

# Project metadata
title: "RadExplain"
description: "A Multi-Agent AI System for Clinical Radiation Oncology Decision Support"

# Disable default theme — we use a fully custom layout
plugins: []

# Markdown parser settings (supports code blocks and tables)
kramdown:
  input: GFM
  syntax_highlighter: rouge
